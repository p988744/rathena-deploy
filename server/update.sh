#!/bin/bash
# ============================================================
# update.sh — 快速重新建置並部署（更新用）
# ============================================================
# 當你修改了設定或想更新 rAthena 版本時使用
# ============================================================

set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR"

echo "🔄 開始更新流程..."
echo ""

# Step 1: 重新建置
echo "===== 建置映像 ====="
./build.sh

echo ""

# Step 2: 重新部署
echo "===== 部署到 VPS ====="
./deploy.sh

echo ""
echo "🎉 更新完成！"
